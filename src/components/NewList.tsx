import React from 'react';
import'./newList.css';

const NewList = () => {
    return (
        <div>
            <button className='newList'>+ add new list</button>
        </div>
    );
};

export default NewList;
